<% provide(:title, 'All books') %>
<p id="notice"><%= notice %></p>
<section style="padding-top: 50px">
	<ul class="list-group">
		<%= render 'grid' %>
	<div class="button">
		<%= link_to "Download all to .csv", books_path(format: :csv) %>
	</div>
</section>
</div>
</div><!--end main content-->
